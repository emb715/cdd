# ============================================================================
# CDD-RAG Environment Configuration
# ============================================================================
# Copy this file to .env and configure your settings
# Most settings are in config.yaml - this is for secrets and local overrides

# ============================================================================
# OPENAI COMPATIBLE API (Optional - for AI-powered answers)
# ============================================================================
# Works with OpenAI, Fuelix AI, and other OpenAI-compatible providers
# Get your API key from your provider (e.g., https://www.fuelix.ai/)
# Leave empty if you only want local search (no AI features)

OPENAI_API_KEY=

# API endpoint (set according to your provider)
# OpenAI: https://api.openai.com/v1
# Fuelix AI: https://api.fuelix.ai/v1
OPENAI_API_BASE=https://api.fuelix.ai/v1

# Default model for AI answers (can also set in config.yaml)
OPENAI_DEFAULT_MODEL=gpt-4o-mini

# ============================================================================
# LOCAL LLM (Alternative to cloud API)
# ============================================================================
# Use local LLM via Ollama instead of cloud API

# Set to 'true' to use local LLM
USE_LOCAL_LLM=false

# Ollama provider (currently only 'ollama' supported)
LOCAL_LLM_PROVIDER=ollama

# Model name (must be pulled in Ollama: ollama pull llama3)
LOCAL_MODEL=llama3

# Ollama API endpoint
OLLAMA_API_BASE=http://localhost:11434

# ============================================================================
# WORKSPACE (Optional - usually set in config.yaml)
# ============================================================================

# Path to CDD workspace (relative to .rag/ directory)
# CDD_WORKSPACE_PATH=../

# ============================================================================
# DEBUGGING (Optional)
# ============================================================================

# Verbose output for debugging
# VERBOSE=false

# Debug mode (very detailed logging)
# DEBUG=false

# Log level: DEBUG, INFO, WARNING, ERROR
# LOG_LEVEL=INFO

# ============================================================================
# NOTES
# ============================================================================
# - Search works 100% offline (no API key needed)
# - AI answers require OPENAI_API_KEY or USE_LOCAL_LLM=true
# - Most configuration is in config.yaml
# - This file is for secrets and environment-specific overrides
# - Don't commit this file with your API key! (already in .gitignore)
